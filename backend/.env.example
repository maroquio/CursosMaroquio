# =============================================================================
# Environment Variables - Example Configuration
# =============================================================================
# Copy this file to .env and adjust values for your environment
# NEVER commit .env to version control!
# =============================================================================

# Application
NODE_ENV=development
PORT=3000

# Admin seed password (REQUIRED in production)
# Use quotes if the password contains # or special chars: ADMIN_PASSWORD="p@ss#word"
ADMIN_PASSWORD=change-me-in-production

# Database (PostgreSQL)
DATABASE_URL=postgresql://app:app@localhost:5435/app

# PostgreSQL Docker settings (used by docker-compose)
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app
POSTGRES_PORT=5435

# JWT Authentication
# IMPORTANT: Change this in production!
JWT_SECRET=your-super-secret-key-change-me-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS
# Use specific origins in production: https://your-frontend.com
CORS_ORIGIN=*

# AI / LLM API Keys
# Required for exercise verification with AI (fill in the ones you use)
ANTHROPIC_API_KEY=your-anthropic-api-key
OPENAI_API_KEY=your-openai-api-key
GOOGLE_API_KEY=your-google-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key

# =============================================================================
# Docker/Production Environment
# =============================================================================

# Docker Registry (optional, for pushing images)
# DOCKER_REGISTRY=your-registry.com

# Image tag (set by CI/CD)
# IMAGE_TAG=latest

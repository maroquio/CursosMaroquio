# =============================================================================
# Docker Compose - Production Environment (CursosMaroquio)
# =============================================================================
# Usage:
#   cp .env.production .env       # Configure environment variables
#   docker compose build           # Build images
#   docker compose up -d           # Start all services
#   docker compose ps              # Check service health
#   docker compose logs -f         # View logs
#   docker compose down            # Stop services
#
# Access: https://cursos.maroquio.com  (CloudPanel → localhost:8702)
# =============================================================================

name: cursos-maroquio

services:
  postgres:
    image: postgres:18-alpine
    container_name: local-postgres
    ports:
      - "${POSTGRES_PORT:-5435}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-maroquio}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-maroquio}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      POSTGRES_MULTIPLE_DATABASES: cursos_maroquio,cursos_maroquio_test
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/scripts/init-multiple-dbs.sh:/docker-entrypoint-initdb.d/init-multiple-dbs.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-maroquio} -d ${POSTGRES_DB:-cursos_maroquio}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cursos-maroquio-backend
    environment:
      NODE_ENV: production
      PORT: 8702
      DATABASE_URL: postgresql://${POSTGRES_USER:-cursos_maroquio}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-cursos_maroquio}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_ACCESS_EXPIRY: ${JWT_ACCESS_EXPIRY:-15m}
      JWT_REFRESH_EXPIRY: ${JWT_REFRESH_EXPIRY:-7d}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://cursos.maroquio.com}
      COOKIE_DOMAIN: ${COOKIE_DOMAIN:-.maroquio.com}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
    volumes:
      - uploads-data:/app/uploads
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8702/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    restart: always
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ""
    container_name: cursos-maroquio-frontend
    ports:
      - "8702:8702"
    restart: always
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres-data:
  uploads-data:
